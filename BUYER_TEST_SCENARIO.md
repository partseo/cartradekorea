# 바이어 관점 모의 구매 검증 시나리오 (BUYER_TEST_SCENARIO.md)

본 문서는 실제 해외 바이어(수입 고객)가 사이트에 접속하여 차량을 탐색하고 견적을 수령하기까지의 전체 사용자 여정(User Journey)을 검증하여, UI 레이아웃의 편의성 및 데이터 오작동을 진단하는 모의 시나리오 가이드라인입니다.

---

## 1. 단계별 바이어 기능 검증 시나리오

운영 런칭 전, 테스트 전담자는 해외 바이어 환경을 재현하기 위해 다음 7단계 시나리오를 처음부터 끝까지 수행하며 각 체크 항목의 이상 유무를 점검해야 합니다.

### 1단계: 회원가입 및 역할 선택
- [ ] **가입 절차**: 우측 상단 GNB에서 **Register**를 누르고 테스트용 바이어 계정(예: `buyer-test@example.com`)으로 가입합니다.
- [ ] **역할 설정**: 가입 폼 내에서 회원 유형을 **Buyer**로 지정합니다.
- [ ] **데이터 확인**: 가입 완료 즉시 Supabase `public.profiles` 테이블의 해당 계정 레코드에 `role` 컬럼값이 `'buyer'`로 입력되어 있는지 관리자 도구에서 확인합니다.

### 2단계: 차량 검색 및 복합 필터 모듈 작동
- [ ] **키워드 검색**: 메인 랜딩 검색창 및 인벤토리 목록(`/cars`)에서 현대/기아 브랜드 및 특정 모델명(예: `Avante`)을 검색하고 관련 매물이 정상 표출되는지 점검합니다.
- [ ] **복합 조건 필터**: 가격 슬라이더, 주행거리, 연식, 변속기, 연료 타입 필터를 각각 조작했을 때 해당 조건에 맞는 매물로 카드 목록이 즉시 동적 정렬 및 필터링되는지 확인합니다.

### 3단계: 차량 상세 및 실시간 요율 계산기 구동
- [ ] **상세 페이지 진입**: 목록 카드 중 하나를 선택해 상세 보기(`/cars/[id]`)로 들어갑니다.
- [ ] **FOB / CIF 요율 전환 테스트**:
  - 상세 정보 영역 하단의 견적 계산 셀렉트 박스에서 **수출 대상 국가(GHA/NGA/LBY/VNM)** 및 목적지 **항구**를 선택합니다.
  - 조건 토글(FOB / CIF)을 클릭할 때마다 내륙 운송비와 해상 운임 등이 반영되어 최종 합산 가격이 1달러 단위까지 정확하게 실시간으로 재합산 표시되는지 확인합니다.
- [ ] **신뢰 배지(Trust Badges) 반응형 뷰**:
  - PC 화면 및 모바일 브라우저 화면에서 6대 신뢰 마크(150-Point Inspection 등)가 깨지지 않고 본문과 견적 박스 영역 주변에 깔끔하게 배열되는지 점검합니다.

### 4단계: 견적서(Quote) 요청서 제출
- [ ] **견적 요청 폼 작성**: 상세 페이지 하단의 **Request FOB/CIF Quote** 또는 독립 페이지(`/quote`)로 이동하여 견적 요청 폼을 채웁니다.
  - 선택 차량, 바이어의 WhatsApp 번호, 수입 항구 등의 2단계 연동 셀렉트 박스가 Zod 스키마 검증을 통과하여 정상 제출되는지 확인합니다.
- [ ] **완료 알림**: 제출 완료 후 "Thank you" 팝업이 출력되는지 검사합니다.

### 5단계: 관리자 승인 후 메일 수신 확인
- [ ] **이메일 포맷 수신**: 관리자가 견적을 승인 발송한 후, 바이어 테스트 메일 계정으로 Resend 이메일이 정확한 템플릿 형태로 수신되는지 확인합니다.
- [ ] **본문 내 링크 작동**: 메일 본문에 포함된 견적서/PI 다운로드 주소를 눌렀을 때, Private Storage 보안을 뚫고 1시간 임시 허용의 signed URL 형태로 브라우저에 PDF가 안전하게 팝업되는지 확인합니다.

### 6단계: 마이페이지 관심 스크랩 및 계약 타임라인 모니터링
- [ ] **관심 차량 등록**: 차량 목록 카드에 있는 하트(관심 등록) 버튼을 눌러 스크랩 처리합니다.
- [ ] **마이페이지(`/mypage`) 확인**:
  - 스크랩한 차량이 나의 관심 매물 목록에 정상적으로 누적 노출 및 삭제되는지 검수합니다.
  - 내가 요청한 견적서가 계약 진행 타임라인 단계 배지(`Pending` -> `Reviewing` -> `Sent` -> `Completed`)에 맞추어 실시간으로 타임라인 노드에 불이 켜지며 동기화되는지 모니터링합니다.

---

## 2. 모바일 실기기(iOS / Android) 반응성 체크 항목

해외 바이어의 80% 이상이 모바일 기기로 사이트에 유입됩니다. 실제 스마트폰이나 개발자 도구의 모바일 에뮬레이터를 이용해 다음 요소들을 필수 점검하십시오.

- [ ] **GNB 모바일 햄버거 메뉴 Drawer**:
  - 우측 상단 삼선 햄버거 아이콘을 터치했을 때 좌/우측에서 드로어가 부드럽게 미끄러지며 노출되고, 언어/통화 전환 셀렉터가 모바일 터치 환경에서 겹침 현상 없이 부드럽게 터치 오버레이되는지 확인합니다.
- [ ] **차량 상세화면 하단 Sticky Action Bar**:
  - 모바일 세로 화면 기준, 페이지 아래쪽에 **FOB/CIF 견적 요청** 및 **WhatsApp 실시간 문의** 두 개의 액션 단추가 고정(Sticky Bottom)되어 화면 스크롤 중에도 언제든지 손가락 터치 범위 내에 자리 잡고 있는지 확인합니다.
- [ ] **목록 페이지 필터 슬라이드오버 Drawer**:
  - 모바일 해상도에서 화면 공간 부족으로 필터가 본문을 가리는지 점검합니다. 필터 보기가 아이콘으로 축소되고, 클릭 시에만 전체 화면을 덮는 Drawer 형식으로 슬라이드 인(Slide-in)되어 노출되는지 체크합니다.

---

## 3. 해외 바이어 특화 편의성 및 통신 환경 검증 (Global Usability)

해외 열악한 네트워크 환경 및 디바이스 특성을 고려한 전용 품질 검증 테스트 항목입니다.

- [ ] **WhatsApp 다이렉트 상담 연결**:
  - GNB, 상세 페이지, 하단 Sticky Bar 등 플랫폼 내 모든 WhatsApp 링크 클릭 시, 지정된 국가코드가 포함된 정상 번호 포맷(e.g., `https://wa.me/821000000000`)으로 연결되며 모바일 WhatsApp App이 오차 없이 호출되는지 검증합니다.
- [ ] **다국어(Language) 세션 유지 상태**:
  - 스페인어(`es`) 또는 아랍어(`ar`)로 언어를 변경한 상태에서 브라우저를 **새로고침(F5)** 하거나 마이페이지 진입 시, 다국어 사전 로드가 풀리지 않고 정상적으로 언어가 유지되어 표출되는지 검사합니다 (LocalStorage 방어 작동 검사).
- [ ] **통화(Currency) 전환 가격 일관성**:
  - USD에서 EUR 또는 AED로 통화를 전환한 후 인벤토리에서 차량 상세 페이지로 이동했을 때, 상세 페이지의 차량 가격이 USD 기본가로 초기화되지 않고 바이어가 지정했던 통화 기호와 연산 결과가 일관되게 표시되는지 확인합니다.
- [ ] **PDF 견적서 모바일 뷰어 다운로드**:
  - 아이폰(Safari) 및 안드로이드(Chrome) 모바일 환경에서 이메일 링크를 통해 signed URL로 진입했을 때, 별도의 PDF 다운로더 앱 설치 요구 없이 모바일 브라우저 자체 뷰어 내에서 견적서 서류가 깨짐 없이 로드되고 저장되는지 검증합니다.
- [ ] **저속 모바일 네트워크 환경 시뮬레이션**:
  - 개발자 도구(Chrome DevTools)의 Network Throttling 기능을 **Fast 3G** 혹은 **Slow 3G**로 설정한 후 접속하여, 5MB 이하의 차량 외관 WebP 이미지들이 다운로드 지연으로 페이지가 멈추는(Freezing) 현상 없이 점차적으로 렌더링(Progressive Loading)되는지 점검합니다.
